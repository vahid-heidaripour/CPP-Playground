- It creates objects for you, rather than you initiating objects directly
- FDP is also known as "Virtual Constructor"
- C++ doesn't support virtual constructor
- It will create objects at runtime not compile time
